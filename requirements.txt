# Core
flask==3.0.3
flask-login==0.6.3
python-dotenv==1.0.1
gunicorn==22.0.0
requests>=2.31.0
telethon>=1.42.0

# Speech — STT (lightweight)
# Uses Google Web Speech via speech_recognition; no local Whisper/Torch models.
SpeechRecognition>=3.10.4

# Speech — TTS
pyttsx3==2.90

# Google OAuth + Gmail API
google-auth==2.29.0
google-auth-oauthlib==1.2.0
google-auth-httplib2==0.2.0
google-api-python-client==2.131.0

# Audio processing
# Browser captures audio; PyAudio is not required for server-side transcription.

# ── Milestone 4: Multi-language support ─────────────────────────────────────
gTTS>=2.5.1          # Google TTS — Hindi / Marathi offline-free audio
deep-translator>=1.11.4  # Google Translate wrapper — response translation

# ── Milestone 3 additions ─────────────────────────────────────────────────────

# Feature 2: Smart Text Summarization
# Simple mode needs no extra packages (built-in only).
# For transformer-based summarization (optional, ~1.6 GB model download):
#   pip install transformers torch
# Uncomment the lines below when you want HuggingFace summarization:
# transformers>=4.40.0
# torch>=2.2.0
